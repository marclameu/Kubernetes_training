import jenkins.plugins.docker.workflow.*

pipeline {
    agent any

    stages {
        stage('show current directory content') {
            steps {
                sh 'ls -la'
            }
        }
        stage('Build Image') {
            steps {
                script {
                    def dockerApp = docker.build("marclameu/users-api", '-f kub-network-01-starting-setup/users-api/Dockerfile .')
                }
            }
        }
    }
}